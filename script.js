//Entrada de dados
let ladoQuadrado = Number(prompt("Digite o comprimento do lado do quadrado"));

//Processamento de dados
let area = ladoQuadrado ** 2;
//let area = Math.pow(ladoQuadrado, 2);
let mensagem = `A área do quadrado é ${area}`;

//Saída de dados
alert(mensagem);